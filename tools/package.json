{
  "name": "psy-tests-platform-tools",
  "version": "2.1.0",
  "description": "SDJ V2 Seven Patterns question generation and validation tools",
  "scripts": {
    "generate-csv": "ts-node generate_sdj_v2_csv.ts",
    "validate-csv": "ts-node validate_csv.ts",
    "build-csv": "npm run generate-csv && npm run validate-csv"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.0"
  }
}
